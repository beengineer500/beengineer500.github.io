# beengineer500.github.io — 에이전트 지침

이 저장소에서 글을 쓰거나 고칠 때 따라야 할 규칙. `CLAUDE.md`는 이 파일의 심볼릭 링크다.

- **구조·빌드 절차** → `README.md`
- **색·타이포·컴포넌트** → `DESIGN.md`
- **글쓰기 규칙과 빌드 함정** → 이 파일

---

## 1. 글쓰기 규칙

### 1.1 쓰지 않는 문장

**비트 하나하나가 아깝다.** 정보를 담지 않은 문장은 전부 뺀다.

| 유형 | 실제로 걸렀던 예 |
|------|------------------|
| 자기 글 칭찬 | "임베딩의 진짜 재미는 여기부터입니다", "어텐션 전체에서 제일 예쁜 대목" |
| 뒤 문장이 할 말 예고 | "여기서 어텐션의 본질이 드러납니다", "이게 왜 중요한지 보겠습니다" |
| 독자에게 글 구성 설명 | "이 글은 ~를 다룹니다", "이게 이 글의 뼈대고요", "마지막에 숙제를 남겨뒀습니다" |
| 정보 없는 부사 | 놀랍도록, 극적으로, 딱, 한눈에, 반드시 |
| 앞말 되풀이 | "다시 한 줄씩만 불러오겠습니다", "정리하면 ~ (이미 정리한 뒤)" |

판단 기준 하나: **그 문장을 지웠을 때 독자가 잃는 정보가 있는가.** 없으면 뺀다.

### 1.2 제목

내용 중심 명사구로 쓴다. 대화체·질문형 금지.

```
✗  잠깐, 이 벡터로 뭘 할 수 있나        ✓  텐서의 세 가지 성질
✗  왜 하나로 묶어 계산하나              ✓  묶인 QKV 가중치 - 768 × 2304
✗  헤드 하나로는 부족하다               ✓  멀티 헤드 어텐션(MHA)
✗  768은 왜 못 바꾸나                   ✓  768을 바꿀 수 없는 이유
```

- 제목만 읽어도 그 절의 내용이 잡혀야 한다
- 숫자가 있으면 제목에 올린다 (`768 ÷ 12 = 64`, `50,257개`)
- 보조 설명은 하이픈으로 붙인다 (`GPT - 디코더 전용 트랜스포머`)

### 1.3 어투

- **합니다체.** 문어체 `~이다`로 쓰지 않는다.
- **`~죠.` 남발 금지.** 짧게 끊는 자리에만 쓰고 **1만 자당 6회를 넘기지 않는다.** 발행본 실측이 1.3~6.3회다.
- **청유형은 `~겠습니다`.** `봅시다`/`갑시다` → `보겠습니다`/`가겠습니다`. 다만 `~라고 해봅시다` 같은 수식 전개용 관용구는 예외.

### 1.4 콜아웃은 용어에만

콜아웃(`<aside class="callout">`)은 **"건너뛰어도 흐름이 이어진다"** 는 신호다. 그래서 용어 정의에만 쓴다.

```markdown
<aside class="callout">
<p class="eyebrow">(용어) 은닉 상태</p>

트랜스포머에서도 쓰이는 말인데 뜻이 조금 다릅니다.

- **RNN의 은닉 상태** - ...
- **트랜스포머의 은닉 상태** - ...

</aside>
```

`<aside>` 태그와 내용 사이에 **빈 줄이 없으면 마크다운이 파싱되지 않는다.** 닫는 태그 앞에도 빈 줄.

**주의·정정·한정은 콜아웃으로 빼지 말고 본문 문장으로 쓴다.** 바로 앞 문장을 고쳐 말하는 내용이라 흐름의 일부다. 박스에 넣으면 "안 읽어도 된다"는 잘못된 신호를 준다. 괄호로 묻는 것도 같은 이유로 피한다.

### 1.5 불릿

주장은 굵게 상위에, 근거는 하위에 중첩한다.

```markdown
- **초반 정보가 희석된다**
  - 그릇 크기는 그대로인데 문장이 길어질수록 계속 덮어씁니다.
```

개념 설명 뒤에는 `**왜 중요한가** - ...` 로 의미를 붙인다.

### 1.6 설명 방식

- **추상 비유보다 구체 예시.** "경사가 급한 쪽으로 굴러간다"보다 실제 토큰이 든 표 한 장이 낫다.
- 흐름 요약은 **코드블록 텍스트 다이어그램**으로.
- 어려운 대목은 `현재 상태 → 목표 상태` 그림 → 한 문장 요약 → `무엇을 + 왜` 순서로 푼다.

### 1.7 시리즈 네비게이션 금지

글 끝에 `1. 제목 / 2. 제목 (현재 글)` 같은 목록을 **붙이지 않는다.** 시리즈 구분은 글 제목으로만 한다.

### 1.8 긴 글의 구조

1만 자가 넘는 글은 책처럼 2단계로 나눈다.

- **부 = H2** — `1부. 어텐션의 등장 배경`
- **절 = H3** — `1.2 RNN의 방식과 한계`
- 절 안에서 더 쪼개야 하면 H4를 쓴다. 목차에는 올리지 않는다.

본문 맨 위에 **수동 목차**를 둔다. 마크업과 앵커 규칙은 2.4절.

닫는 세 섹션은 고정이다.

- **전체 흐름 정리** — 코드블록 다이어그램 하나로 전체를 다시 훑고, `기억할 숫자` 표로 닫는다
- **막혔던 곳** — `**질문?** 답변` 형식으로 5개 이상. **실제로 걸린 것만 쓴다.** 요약본은 널렸고 이 섹션이 이 블로그의 차별점이라, 지어내면 존재 이유가 사라진다
- **출처** — 소스마다 타임스탬프 링크

분량 기준은 **공백 제외 12,000자 이상**. 측정은 frontmatter를 뺀 본문에서 HTML 태그와 주석을 걷어낸 뒤 공백을 제거한 길이.

**화면이나 자료에 있는 실제 숫자를 본문에 쓴다.** `768 × 2304`, `(6, 64)`, `−137.12 ÷ 0.8 = −171.40` 처럼. 추상적으로 "행렬을 곱한다"고 쓰는 것보다 독자가 그림과 본문을 대조할 수 있다.

### 1.9 시리즈로 쓸 때

- **제목에 시리즈 이름과 번호를 붙인다.** `트랜스포머 1편. 텍스트가 벡터가 되기까지 - 토큰화와 임베딩`. 시리즈가 둘 이상이면 맨 `1편.`만으로는 목록 페이지에서 어느 시리즈인지 알 수 없다. 시리즈 이름은 slug의 접두어와 맞춘다(`transformer-01-…` → `트랜스포머 1편.`).
- **slug도 번호로 시작시킨다.** `transformer-01-tokenize-embedding`. 목록 페이지는 날짜 내림차순으로만 정렬하고 같은 날짜 안에서는 **폴더 이름 순서**를 그대로 쓰기 때문에, 한날 여러 편을 올리면 slug 번호가 곧 표시 순서가 된다.
- **앞 편이 다룬 것은 다시 설명하지 않는다.** `2편 3.3절에서 본 그 Softmax` 처럼 **절 번호까지 짚어** 참조만 하고, 그 편에서 새로운 것만 쓴다.
- **예고한 것은 회수한다.** 앞 편에서 "4편에서 다시 만납니다"라고 썼으면 해당 편에서 실제로 이어받는다.

---

## 2. 빌드가 깨지는 지점

`scripts/build.mjs`는 `marked` + 자체 프론트매터 파서다. 아래를 어기면 빌드가 실패하거나 화면이 깨진다.

### 2.1 frontmatter

```yaml
---
title: 텍스트가 벡터가 되기까지 - 토큰화와 임베딩
description: GPT-2가 입력 문장을 어떻게 숫자로 바꾸는지 따라갑니다
date: 2026-08-09
category: llm
tags: transformer, embedding, tokenization, gpt-2
---
```

- 5개 전부 필수. 하나라도 없으면 빌드 실패.
- **값에 `:` `"` `<` `>` 를 쓰지 않는다.** 파서가 첫 콜론으로만 자르고, 값이 HTML 속성에 그대로 들어간다.
- `tags`는 YAML 리스트가 아니라 **콤마로 구분한 한 줄**.
- `category`는 하나, `tags`는 여러 개.

### 2.2 헤딩

- **H1(`#`) 금지.** 페이지 `<h1>`은 `title`이 차지한다.
- H2 = 부, H3 = 절. 자동 목차와 섹션 분할이 H2를 기준으로 동작한다.

### 2.3 이미지는 평면 배치

빌더가 `copyFileSync`로 **파일만** 복사한다. 포스트 폴더에 하위 디렉터리가 있으면 `EISDIR`로 죽는다.

```
✗  ![](attachments/foo.png)      ✓  ![](foo.png)
```

### 2.4 수동 목차

긴 글은 본문 상단에 목차를 직접 둔다. `id="manual-toc"` 가 있으면 `main.js`가 자동 목차를 만들지 않는다.

- **H2 링크는 `#section-N`** — N은 문서 안 H2 등장 순서(1부터). `### 목차`는 H3라 카운트에서 빠진다.
- **H3 링크는 앵커를 직접 심는다.** 각 H3 바로 위에 `<a id="s2-3"></a>`.

**제목을 바꾸면 목차 라벨도 같이 바꾼다.** 헤딩만 고치면 앵커는 살아 있어서 **링크 검증으로 안 잡히는** 조용한 불일치가 생긴다. 실제로 한 번 발생했다.

---

## 3. 이미지 처리

WebP, 가로 1400px(본문 폭 700px의 2배), 품질 82.

```bash
sips --resampleWidth 1400 raw.png --out tmp.png
cwebp -q 82 tmp.png -o figure-name.webp
```

- 파일명은 의미 있게 (`02-qkv-weights.webp`). 도구가 뱉은 ULID를 그대로 쓰지 않는다.
- `alt`는 필수. 화면에 무엇이 있는지 적는다.
- `<figure>` + `<figcaption>` 으로 감싸고, 캡션 끝에 출처 링크를 단다.
- 스크린샷은 대부분 흰 배경이라 투명 배경 export는 의미가 없다. `.article figure img` 의 테두리가 다크 테마에서 카드처럼 앉게 해준다.

---

## 4. 인용과 저작권

출처 표기는 **필요조건이지 충분조건이 아니다.** 정당한 인용을 가르는 건 분량과 주종관계다.

- **남의 영상·강의 캡처는 글 하나당 3~5장이 기본.** 캡처를 시간순으로 늘어놓으면 원본의 대체재가 되어 인용 범위를 벗어난다.
- **직접 그린 도식과 오픈소스 도구 화면을 우선 쓴다.** (예: Excalidraw 자작 도식, Transformer Explainer)
- 캡션과 `출처` 섹션에 **출처 + 타임스탬프 링크**를 단다. 원본으로 트래픽을 보내는 구조를 유지한다.
- 상업 강의 자료 비중이 커지면 저작자에게 한 줄 물어보고 쓴다.

### 3~5장을 넘겨도 되는 경우

원본이 **애니메이션으로 개념을 전개하는 영상**이라 정지 화면 한두 장으로는 논지가 서지 않을 때가 있다. 이럴 땐 넘겨도 되지만, 아래를 **전부** 만족해야 한다.

- **캡션이 해석을 한다.** 화면에 뭐가 있는지 나열하는 게 아니라, 본문의 논지를 이어받아 "이 그림이 무엇을 증명하는가"를 쓴다. 캡션을 지웠을 때 그림이 붕 뜨면 그냥 나열이다.
- **그림마다 맡은 논점이 다르다.** 같은 얘기의 연속 프레임을 여러 장 넣지 않는다. 한 장이 한 절을 책임진다.
- **본문이 압도적으로 주(主)다.** 글을 지우고 그림만 남겼을 때 원본 영상의 요약본이 되면 안 된다.
- **타임스탬프 링크를 캡션마다** 단다. 독자가 해당 지점을 바로 열 수 있어야 한다.

실제 적용 사례로 `4편. 서빙에서 어텐션 비용 줄이기`가 캡처 7장을 쓴다. KV Cache의 쿼리 독립성이 **점선 상자가 나뉘는 그림 없이는 글로 설명되지 않는** 대목이라, 얽힘(K·V)과 독립(Q)을 대비하는 두 장을 나란히 넣었다. 본문은 14,000자가 넘고 캡션마다 논점과 타임스탬프가 붙어 있다.

반대로 **정지 화면만으로 충분한 소스**(슬라이드 강의, 문서, 웹 도구)는 예외에 해당하지 않는다. 기본 3~5장을 지킨다.

---

## 5. 발행 절차

```
content/drafts/<slug>/           작성 중. .gitignore 대상이라 커밋되지 않는다
        ↓ 완성되면 폴더째 이동
content/posts/<year>/<slug>/     index.md + 이미지(평면)
        ↓ npm run build
posts/<year>/<slug>/index.html   + posts/, categories/, tags/ 목록 페이지 자동 생성
        ↓ git commit → git push
GitHub Pages 공개
```

- **목록 페이지는 전부 자동 생성된다.** 손으로 고치지 않는다. 고쳐도 다음 빌드에 덮인다. `feed.xml`(RSS)과 홈의 최신 글 목록도 같이 갱신된다.
- **새 카테고리·태그를 쓰면** `content/taxonomy.json`에 설명을 추가한다. 없으면 `llm 카테고리에 속한 메모입니다` 같은 자동 문구가 나간다.
- **글을 지우면 목록 페이지는 자동으로 안 지워진다.** 그 글이 마지막으로 쓰던 분류·태그가 있으면 `categories/<slug>.html`, `tags/<slug>.html`을 손으로 지운다. 안 지우면 링크만 끊긴 빈 페이지가 남는다.
- 빌드 산출물(`posts/`, `categories/`, `tags/`, `feed.xml`)도 커밋 대상이다. Pages가 저장소 루트를 그대로 서빙한다.
- **CI가 이 동기화를 강제한다.** `.github/workflows/build-check.yml`이 `npm run build` 뒤에 `git diff --exit-code`를 돌린다. 소스만 고치고 빌드물을 안 올리면 CI가 깨진다. **커밋 전에 항상 빌드한다.**
- 푸시하면 즉시 공개된다. 푸시 전에 로컬 확인: `python3 -m http.server 8000`
- 이 저장소는 개인 계정 소유다. 리모트가 `git@github-beengineer500:...` SSH 별칭으로 잡혀 있어야 푸시된다. HTTPS면 회사 계정으로 붙어 403이 난다.
